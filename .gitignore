node_modules/
dist/
.env
server/uploads/

# Form submissions contain personal contact details — never commit
server/data/

# Tesseract downloads lang data into cwd during OCR
*.traineddata

# Agent scratch space (imports, screenshots, headless-Chrome profiles)
.tmp-*/
